// Stub: admin panel renders its own sidebar header. The seller's userHeader
// pulled in too much auth/profile UI. This passes through nothing — the
// admin layout supplies the chrome.
export default function UserHeader() {
  return null;
}
